import 'package:flutter/material.dart';

class TopSeriesPage extends StatelessWidget {
  const TopSeriesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Top 250 series'),
      ),
    );
  }
}
